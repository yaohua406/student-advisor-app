// Archived dashboard: original implementation moved to web/legacy-react
import React from 'react';

const Dashboard = () => (
  <div style={{ padding: 20 }}>
    <h2>Archived Dashboard</h2>
    <p>This dashboard has been archived. See <code>web/legacy-react</code> for the original React implementation.</p>
  </div>
);

export default Dashboard;