// Archived duplicate page — see web/legacy-react
import React from 'react';
const Home = () => (
  <div style={{ padding: 24 }}>
    <h1>Archived App</h1>
    <p>This page has been archived. See <code>web/legacy-react</code> for the React source.</p>
  </div>
);

export default Home;