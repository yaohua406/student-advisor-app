// Archived duplicate dashboard — see web/legacy-react
import React from 'react';
const Dashboard = () => (
  <div style={{ padding: 20 }}>
    <h2>Archived Dashboard</h2>
    <p>See <code>web/legacy-react</code> for the original implementation.</p>
  </div>
);

export default Dashboard;