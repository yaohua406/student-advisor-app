// Archived duplicate layout — see web/legacy-react
import React from 'react';
const Layout: React.FC<any> = ({ children }) => (
  <div style={{ padding: 12 }}>Archived layout — see web/legacy-react</div>
);

export default Layout;